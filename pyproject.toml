[project]
name = "linksnap"
version = "0.1.0"
description = "OMO Teams Quickstart — URL shortener API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "azure-cosmos>=4.7",
    "azure-identity>=1.18",
]
